GCP:
g2-standard-8 OR  a2-highgpu-1g
IAM & Admin → Quotas => NVIDIA L4 GPUs
Boot Disk

Ubuntu 22.04 LTS

100 GB SSD

sudo apt update
sudo apt install -y ubuntu-drivers-common
sudo ubuntu-drivers install
sudo reboot
sudo apt install -y nvidia-cuda-toolkit
pip install vllm

python -m vllm.entrypoints.openai.api_server \
    --model nvidia/Nemotron-8B \
    --dtype auto \
    --gpu-memory-utilization 0.9
pip install torch transformers accelerate

------------------------------

sudo apt purge -y 'nvidia*'
sudo rm -rf /etc/modprobe.d/nvidia*
sudo update-initramfs -u
sudo reboot

sudo apt update
sudo apt install -y nvidia-driver-570-server
sudo reboot

sudo apt purge -y 'nvidia*'
sudo rm -rf /etc/modprobe.d/nvidia*
sudo rm -rf /lib/modules/$(uname -r)/updates/dkms/nvidia*
sudo update-initramfs -u
sudo reboot

lsmod | grep nvidia

az vm extension set \
  --resource-group <your-rg> \
  --vm-name <your-vm-name> \
  --name NvidiaGpuDriverLinux \
  --publisher Microsoft.HpcCompute \
  --version 1.10

az vm extension delete \
  --resource-group <your-rg> \
  --vm-name <your-vm-name> \
  --name NvidiaGpuDriverLinux

dmesg | grep -i hyperv

uname -r

